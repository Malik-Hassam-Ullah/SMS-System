# Git Commit & Push Policy

- Whenever a task or change is approved, accepted, or verified by the user, automatically create a meaningful git commit and push it to GitHub (`origin/main`).
- This ensures all accepted work is safely backed up and increases GitHub commit activity as requested by the user.
- Use concise, conventional commit messages (e.g., `feat: enhance collect fee UI and search functionality`).
